{% extends 'base.j2' %}

{%from 'bootstrap5/form.html' import render_form %}

{% block title %}
Movie rating
{% endblock %}

{% block content %}
<div class="content">
  <h1 class="heading">{{title}}</h1>
  <p class="description">Movie rating</p>
  <form action="{{url_for('update', form=form, id=id)}}" method="POST" novalidate>
    {{render_form(form)}}
  </form>
</div>
{% endblock %}